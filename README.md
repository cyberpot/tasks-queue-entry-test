# How to run

Create `.env` file according to `sample.env`

```bash
cp sample.env .env
```

Build docker image
```bash
docker-compose build
```

Run containers
```bash
docker-compose up
```

Access to swagger
```
http://localhost:8100
```
